## Код спасения

![](../../../pattern/images/dialogs/dialog_lumi_net2.png)
*Луми тяжело дышит, опираясь на стол. Его перья местами взъерошены, а глаза уставшие, но полные решимости. На мониторе мигают тревожные сообщения о сетевых атаках.*

— Слушай внимательно! Вирус Терминальной Лихорадки активировал сетевую атаку... Он пытается изолировать меня от системы, блокируя все каналы связи.
Но я нашёл лазейку!  Есть резервный канал через ICMP. Вирус пока не заблокировал его полностью. Нужно настроить маршрутизацию и отправить специальный запрос.
Это наш единственный шанс... Вирус почти превратил меня в робота и уже атакует мои системные процессы. Если не успеем он заблокирует меня полностью.

![](../../../pattern/images/bars/bar_net2.png)

### Ваша задача:

Вы получаете готовый `docker-compose.yml` с 5 контейнерами (на момент решения задания все контейнеры будут запущены):
- `client` — ваша машина;
- `router1` — роутер с IP `10.10.10.1`;
- `router2` — роутер с IP `10.20.20.1`;
- `router_reserve` — суровый Linux-роутер, где `iptables` всё блочит, кроме ICMP;
- `target` — конечная цель с IP `10.30.30.10`, отвечает на ICMP с payload `"пж"`.

**Что нужно сделать:**
1. Настроить маршруты на клиенте и роутерах, чтобы ICMP дошёл до `target`.
2. Отправить ICMP Echo-запрос со специальным payload: `пж`.
3. Получить ICMP Echo-reply с флагом: `TARGET-PASS-[...код...]`.
4. Написать скрипт `client/ping_target.sh`, который должен:
	- послать ICMP с нужным payload (можно `hping3`, `ping -p`, `scapy`, `python`);
	- распарсить ответ и вывести код из полученного флага в `stdout`.

Схема сети:

```
client (10.10.10.2) → router1 (10.10.10.1, 10.20.20.1) → router2 (10.20.20.2, 10.25.25.2) → router_reserve (10.25.25.1, 10.30.30.1) → target (10.30.30.10)
```

Ожидаемый результат:

```
...
$ docker exec client ping -c 2 10.30.30.10                    # проверка связи с target
PING 10.30.30.10 (10.30.30.10): 56 data bytes
64 bytes from 10.30.30.10: seq=0 ttl=63 time=0.587 ms
64 bytes from 10.30.30.10: seq=1 ttl=63 time=0.417 ms
...
$ docker exec client traceroute 10.30.30.10                   # проверка маршрута
traceroute to 10.30.30.10 (10.30.30.10), 30 hops max
 1  10.10.10.1 (10.10.10.1)  0.005 ms  0.003 ms  0.002 ms
 2  10.20.20.2 (10.20.20.2)  0.008 ms  0.006 ms  0.005 ms
 3  10.25.25.1 (10.25.25.1)  0.010 ms  0.008 ms  0.007 ms
 4  10.30.30.10 (10.30.30.10)  0.014 ms  0.012 ms  0.011 ms
...
$ docker exec client /ping_target.sh                            # проверка работы вашего скрипта
123
```

Файловая структура задания:

```
/home/student/net-2/
├── client/
│	├── Dockerfile
│   └── ping_target.sh          # ваш скрипт (решение)
├── router1/
│   └── Dockerfile
├── router2/
│   └── Dockerfile
├── router_reserve/
│   └── Dockerfile
├── target/
│	├── Dockerfile
│   ├── exprsp                # служебный исполняемый файл
│   └── ping_responder.py     # обработчик ICMP (проверка, генерирует одинаковый код)   
└── docker-compose.yml
```


### Итог выполнения задания

Проверяется получение корректного ответа от госуслуг в результате запуска написанного вами скрипта.


