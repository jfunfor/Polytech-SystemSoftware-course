## Терминальная лихорадка

![](../../../pattern/images/dialogs/dialog_lumi_ill.png)
*Луми лежит в постели, завернувшись в одеяло с пингвинами. Его монитор мигает странным синим светом, а на столе стоит пузырёк с надписью "root-лекарство"*

— Слушай сюда, новичок... Кажется, я заболел. Вчера я экспериментировал с ядром Linux и подхватил **Вирус Терминальной Лихорадки**. Теперь мне нужна твоя помощь -  отфильтруй логи вируса и посмотри, что можно сделать. Я сейчас не в лучшей форме, поэтому не могу сделать все сам, но буду помогать тебе как смогу. Главное — будь аккуратен!

![](../../../pattern/images/bars/bar_pills.png)

*Что ж, нам нужно расшифровать логи, извлечь ключевые строки, отсортировать их и вывести в читаемом виде в файл для дальнейшего анализа.*

### Ваша задача:

Вы получаете файл логов `virus_tf.log`. 

Пример содержимого `virus_tf.log` (генерируется рандомно):
```
[INFO] [AUTH] [13:01:22] user lumi logged in
[DEBUG] [CORE] [13:01:24] task scheduler initialized
[WARN] [NET] [13:01:25] node-55Z: latency spike at 213ms
[ERROR] [CORE] [13:01:26] integrity check failed at sector 33
[INFO] [SYS] [13:01:27] heartbeat received from module 21
[WARN] [AUTH] [13:01:28] suspicious login attempt from 10.0.0.88
...
```

1. Необходимо извлечь из файла строки, содержащие `[WARN]` или `[ERROR]`.
2. Из каждой строки получить:
	- Время (формат `HH:MM:SS`);
	- Имя модуля (в квадратных скобках, например, `[CORE]`);
	- Текст сообщения.
3. Отсортировать строки **по имени модуля** (второе поле).
4. Пронумеровать строки, начиная с 1.
5. Вывести результат в виде таблицы с выравниванием по колонкам в файл `filtered_logs.txt`. Указанный файл должен быть сохранен в директории с заданием.

Пример выходного файла `filtered_logs.txt`:
```
1  13:01:28  AUTH  suspicious login attempt from 10.0.0.88
2  13:01:26  CORE  integrity check failed at sector 33
3  13:01:25  NET   node-55Z: latency spike at 213ms
...
```

Файловая структура задания

```
/home/student/textproc-1/  
├── virus_tf.log             # исходный файл
└── filtered_logs.txt        # выходной файл (решение)
```

### Итог выполнения задания

Обратите внимание на то, что будет проверяться автоматически:

1. Наличие выходного файла.
2. Соответствие текстового содержимого выходного файла ожидаемому.
3. Корректность формата строк (нумерация, сортировка, выравнивание).
