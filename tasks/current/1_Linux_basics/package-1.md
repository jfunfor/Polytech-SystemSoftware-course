## Код анти-мутации

![](../../pattern/images/dialogs/dialog_lumi_glitch.png)
*Комната Луми погружена в полумрак. Синий свет монитора отбрасывает тревожные блики на фигуру пингвиненка. Он лежит неподвижно, его ласты неестественно блестят, а кожа местами отливает металлическим оттенком.*

— Ты вовремя... Вирус... он не просто лихорадка... Это оружие. Оно переписывает мой код... Превращает в машину...Нет времени объяснять! Система уязвима... Он проник через старые репозитории, через незащищенные порты... Тебе нужно срочно заблокировать все точки входа! 
Проверь все пакеты... Все источники! Настрой APT так, чтобы никакой неподписанный код не прошел! Это мой единственный шанс остановить мутацию.

![](../../pattern/images/bars/bar_glitch.png)

### Ваша задача:

1. Обновите список пакетов и саму систему.
2. Установите следующие утилиты:
	- `curl`;
	- `ufw`;
	- `fail2ban`.
3. Выведите список всех установленных пакетов и сохраните его в файл `/var/log/pkg-list.log`.
4. Удалите пакет `telnet`, если он установлен.
5. Настройте APT таким образом, чтобы он не разрешал установку пакетов из неподписанных репозиториев.
6. Проверьте, какие репозитории используются системой, и сохраните список в файл `/var/log/repos.log`.

**Ожидаемый результат:**

Файловая структура логов:
```
/var/log/
├── pkg-list.log      # Список всех пакетов
└── repos.log         # Список репозиториев
```

Установленные пакеты (версии могут отличаться):
```
$ dpkg -l | grep -E "curl|ufw|fail2ban"
ii  curl      7.81.0-1ubuntu1.20   amd64  
ii  ufw       0.36.1-4ubuntu0.1    all
ii  fail2ban  0.11.2-1             all

$ dpkg -l | grep telnet
(пусто)
```

### Итог выполнения задания

Обратите внимание на то, что будет проверяться автоматически:

1. Установка указанных утилит.
2. Отсутствие указанного пакета.
3. Файлы со списками пакетов и репозиториев.
4. Настройки подписи APT.