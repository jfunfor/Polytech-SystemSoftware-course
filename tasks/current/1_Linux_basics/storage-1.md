## Пингвинье послание

![](../../../pattern/images/dialogs/dialog_lumi_ill_3.png)
*Луми кое-как подпирает спину подушками. На коленях у него неуклюже балансирует планшет, а ласта с трудом попадает по клавишам.*

— Хех... Приветствие от нас обоих получается какое-то... сопливое. Я всё ещё чихаю синтаксическими ошибками, а система приветствует нас стандартным motd. Скучно! Давай сделаем что-то... теплое. 
Пингвинье...
Напишем скрипт, который будет выводить на всех терминалах забавное сообщение через cowsay! И заодно научимся управлять этим через systemd. Чтобы в следующий раз, если я снова подхвачу какую-нибудь лихорадку, система сама могла оповестить всех, что нужно срочно помочь бедному пингвину! 
Справишься? 

![](../../../pattern/images/bars/bar_tears.png)

### Ваша задача:

1. Имеется виртуальный диск `/dev/sdb`. Разметьте его вручную:
	- `sdb1` — 500MB, под `/boot`, файловая система `ext2`;
	- `sdb2` — 1GB, под swap;
	- `sdb3` — остальное, под корневую файловую систему `/`, использовать `ext4`.
2. Отформатируйте каждую файловую систему соответствующим образом, а затем смонтируйте:
	- `sdb1` → `/mnt/vaccine/boot`;
	- `sdb3` → `/mnt/vaccine/`;
	- swap — активировать.
3. Добавить автоматическое монтирование, внеся записи в `/etc/fstab` для всех разделов.

**Ожидаемый результат в формате вывода команд:**

```
$ fdisk ...
Device     Boot    Start      End  Sectors  Size Id Type
/dev/sdb1           2048  1026047  1024000  500M 83 Linux
/dev/sdb2        1026048  3123199  2097152    1G 82 Linux swap
/dev/sdb3        3123200 41943039 38819840 18.5G 83 Linux

$ ...
NAME   FSTYPE LABEL UUID                                 MOUNTPOINT
sdb                                                      
├─sdb1 ext2         a1b2c3d4-1234-5678-9101-112131415161 /mnt/vaccine/boot
├─sdb2 swap         1a2b3c4d-5678-9101-1121-314151617181 [SWAP]
└─sdb3 ext4         e5f6g7h8-9101-1121-3141-516171819202 /mnt/vaccine

$ ...
/dev/sdb1 on /mnt/vaccine/boot type ext2 (rw,relatime)
/dev/sdb3 on /mnt/vaccine type ext4 (rw,relatime)

$ ...
NAME      TYPE      SIZE USED PRIO
/dev/sdb2 partition   1G   0B   -2
```

Это пример, у вас будут другие размеры и UUID.

### Итог выполнения задания

Обратите внимание на то, что будет проверяться автоматически:

1. Корректность файловых систем.
2. Наличие и монтирование директорий.
3. Корректность `/etc/fstab`.
4. Активный swap.