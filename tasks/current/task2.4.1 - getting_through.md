# Тема Маршрутизация. Задание 1. Чем дальше в сеть.

Вы — нетраннер из подпольной группировки ZeroTrace. Вам необходимо подключить изолированный терминал старой станции связи к хакерской сети через цепочку из маршрутизаторов, избегая цифровой слежки. Только грамотная настройка маршрутизации позволит пробиться сквозь глухие узлы.

## Условие задания

Вы получаете готовый `docker-compose.yml` с 3 контейнерами (на момент решения задания все контейнеры будут запущены):
- `node-a` — хост, нуждающийся в маршруте, интерфейс `eth0: 10.0.1.10/24` ;
- `node-b` — маршрутизатор, интерфейсы `eth0: 10.0.1.1/24` и `eth1: 10.0.2.1/24`;
- `node-с` — целевая система, интерфейс `eth0: 10.0.2.20/24`.

Необходимо настроить маршруты и включить IP-форвардинг так, чтобы трафик из изолированной сети (`node-a`) проходил через промежуточный маршрутизатор (`node-b`) и достигал внешней сети (`node-c`).

**Что нужно сделать:**
1. На `node-a`:
	- Добавьте статический маршрут для подсети `10.0.2.0/24` через шлюз `10.0.1.1`.
2. На `node-b`:
	- Убедитесь, что IP-форвардинг включен (временно и в `sysctl.conf`);
	- Убедитесь, что IP-таблица маршрутов корректна (ничего не нужно добавлять вручную, если все IP настроены верно).
3. На `node-a`:
	- Добавьте дефолтный маршрут через `10.0.1.1`.
4. Проверка соединения:
	- Убедитесь, что после настройки проходит `ping 10.0.2.20` с `node-a`.
	- Для проверки правильности цепочки запретите ICMP с `node-b` → `node-c`.

**Схема сети**

```
node-a (10.0.1.10) --- (10.0.1.1) node-b (10.0.2.1) --- (10.0.2.20) node-c
```

**Ожидаемый результат**
```
# На node-a:
$ docker exec node-a ip route
10.0.1.0/24 dev eth0 
10.0.2.0/24 via 10.0.1.1 dev eth0 
default via 10.0.1.1 dev eth0
...
# Проверка связи:
$ docker exec node-a ping -c 2 10.0.2.20
PING 10.0.2.20 (10.0.2.20): 56 data bytes
64 bytes from 10.0.2.20: seq=0 ttl=63 time=0.123 ms
64 bytes from 10.0.2.20: seq=1 ttl=63 time=0.096 ms
...
# Проверка маршрута:
$ docker exec node-a traceroute -n 10.0.2.20
 1  10.0.1.1  0.005 ms
 2  10.0.2.20  0.010 ms
```

## Файловая структура задания

```
/home/student/task2.4.1/
├── node-a/
│   └── Dockerfile
├── node-b/
│   └── Dockerfile
├── node-c/
│   └── Dockerfile 
└── docker-compose.yml
```

## Автоматическая проверка 

Обратите внимание на то, что будет проверяться автоматически:

1. Корректность маршрутов.
2. Доступность `node-с`.

